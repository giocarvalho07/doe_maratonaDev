document/* documento global html */
    .querySelector('header button') /* pesquisar por seletores css */
    .addEventListener("click", function()/* adicionar um evento de clicar */{
            document
                .querySelector('.form')/* selecionar o form*/
                .classList.toggle('hide')/* Classlist : ao clicar vamos ter a opção de criar classes que não foram criadas diretamente no html */
                /* toggle = adicionar ou remover conforme o parametro */
})